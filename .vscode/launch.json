{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Executar Script: start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}\\backend"
        }
    ]
}